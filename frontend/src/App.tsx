import { RouterProvider } from 'react-router-dom'
import { router } from './app/router'

function App() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Study Buddy Match</h1>
      <p>Frontend skeleton (React + Vite + TypeScript)</p>
    </main>
  )
}

export default App
